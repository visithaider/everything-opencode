The plugin is built and ready. Here's the publish command:
cd .opencode && npm publish
Prerequisites:
1. You need an npm account (npm login)
2. The package name is ecc-opencode
To publish with a specific tag or scope:
cd .opencode && npm publish --access public
If you want to test locally first without publishing to npm:
cd .opencode && npm pack


